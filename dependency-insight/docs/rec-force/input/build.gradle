apply plugin: 'java'

repositories {
    jcenter()
    maven { url 'repo' }
}

dependencies {
    compile 'com.google.guava:guava'
    compile 'sample:bom:1.0.0'
}

configurations.all {
    resolutionStrategy {
        force 'com.google.guava:guava:14.0.1'
    }
}
