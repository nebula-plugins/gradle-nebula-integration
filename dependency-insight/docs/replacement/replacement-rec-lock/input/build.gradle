apply plugin: 'java'

repositories {
    jcenter()
    maven { url 'repo' }
}

dependencies {
    compile platform('sample:bom:1.0.0')
    compile 'com.google.guava:guava'
}

dependencyLocking {
    lockAllConfigurations()
}
