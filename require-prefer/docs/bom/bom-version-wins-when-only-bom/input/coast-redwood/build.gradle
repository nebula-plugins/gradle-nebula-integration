
plugins {
    id 'java-library'
}

group 'tree'
version '1.0'

dependencies {
    api platform('sample:bom:1.0.0')
    api 'tree:acacia'
    api ('tree:acacia') {
        version {
            prefer '1.6'
        }
    }
}

repositories {
    maven { url '../../../../../repo/maven-repo' }
}
